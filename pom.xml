<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>de.pocketcloud</groupId>
    <artifactId>cloud-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>PocketCloud Parent</name>
    <description>Parent POM for PocketCloud</description>

    <modules>
        <module>cloud-common</module>
        <module>cloud-api</module>
        <module>cloud-shared</module>
        <module>cloud-network</module>
        <module>cloud-core</module>
        <module>cloud-bridge</module>
    </modules>

    <properties>
        <!-- Build -->
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- Plugin versions -->
        <compiler-plugin.version>3.13.0</compiler-plugin.version>
        <lombok.version>1.18.46</lombok.version>

        <!-- Netty -->
        <netty.version>4.2.15.Final</netty.version>

        <!-- Jackson -->
        <jackson.version>3.2.1</jackson.version>
        <jackson-annotations.version>2.22</jackson-annotations.version>

        <!-- Logging -->
        <slf4j.version>2.0.18</slf4j.version>
        <logback.version>1.5.38</logback.version>

        <!-- Game-Server-Dependencies -->
        <powernukkitx.version>nightly-SNAPSHOT</powernukkitx.version>
        <waterdog.version>2.0.4-SNAPSHOT</waterdog.version>

        <!-- Other external Libraries -->
        <okaeri-configs.version>5.0.13</okaeri-configs.version>
        <jetbrains-annotations.version>26.1.0</jetbrains-annotations.version>
        <commons-compress.version>1.28.0</commons-compress.version>
        <commons-lang3.version>3.20.0</commons-lang3.version>
        <hikaricp.version>7.0.2</hikaricp.version>
        <jline.version>3.30.6</jline.version>
        <discord-webhook.version>1.0.2</discord-webhook.version>
        <gson.version>2.14.0</gson.version>
        <oshi.version>7.4.1</oshi.version>
        <reflections.version>0.10.2</reflections.version>

        <!-- Testing -->
        <junit.version>6.1.1</junit.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- ================= Lombok ================= -->
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </dependency>

            <!-- ================= Netty ================= -->
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-common</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-buffer</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-transport</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-codec</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-handler</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-codec-http</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-transport-native-epoll</artifactId>
                <version>${netty.version}</version>
            </dependency>
            <dependency>
                <groupId>io.netty</groupId>
                <artifactId>netty-pkitesting</artifactId>
                <version>${netty.version}</version>
            </dependency>

            <!-- ================= Jackson ================= -->
            <!-- Jackson 3: Core/Databind unter tools.jackson.*, Annotations weiterhin unter com.fasterxml.jackson.* -->
            <dependency>
                <groupId>tools.jackson.dataformat</groupId>
                <artifactId>jackson-dataformat-yaml</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>tools.jackson.core</groupId>
                <artifactId>jackson-databind</artifactId>
                <version>${jackson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fasterxml.jackson.core</groupId>
                <artifactId>jackson-annotations</artifactId>
                <version>${jackson-annotations.version}</version>
            </dependency>

            <!-- ================= Logging ================= -->
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-simple</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>${logback.version}</version>
            </dependency>

            <!-- ================= Game-Server-Dependencies ================= -->
            <dependency>
                <groupId>org.powernukkitx</groupId>
                <artifactId>server</artifactId>
                <version>${powernukkitx.version}</version>
            </dependency>
            <dependency>
                <groupId>dev.waterdog.waterdogpe</groupId>
                <artifactId>waterdog</artifactId>
                <version>${waterdog.version}</version>
            </dependency>

            <!-- ================= Other Libraries ================= -->
            <dependency>
                <groupId>eu.okaeri</groupId>
                <artifactId>okaeri-configs-yaml-snakeyaml</artifactId>
                <version>${okaeri-configs.version}</version>
            </dependency>
            <dependency>
                <groupId>org.jetbrains</groupId>
                <artifactId>annotations</artifactId>
                <version>${jetbrains-annotations.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-compress</artifactId>
                <version>${commons-compress.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>
            <dependency>
                <groupId>com.zaxxer</groupId>
                <artifactId>HikariCP</artifactId>
                <version>${hikaricp.version}</version>
            </dependency>
            <dependency>
                <groupId>org.jline</groupId>
                <artifactId>jline</artifactId>
                <version>${jline.version}</version>
            </dependency>
            <dependency>
                <groupId>de.pocketcloud</groupId>
                <artifactId>discord-webhook</artifactId>
                <version>${discord-webhook.version}</version>
            </dependency>
            <dependency>
                <groupId>com.google.code.gson</groupId>
                <artifactId>gson</artifactId>
                <version>${gson.version}</version>
            </dependency>
            <dependency>
                <groupId>com.github.oshi</groupId>
                <artifactId>oshi-core</artifactId>
                <version>${oshi.version}</version>
            </dependency>
            <dependency>
                <groupId>org.reflections</groupId>
                <artifactId>reflections</artifactId>
                <version>${reflections.version}</version>
            </dependency>

            <!-- ================= Testing ================= -->
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-api</artifactId>
                <version>${junit.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${compiler-plugin.version}</version>
                <configuration>
                    <source>25</source>
                    <target>25</target>
                    <encoding>UTF-8</encoding>
                    <release>25</release>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>